export function LocalBusinessJsonLd() {
  // Google LocalBusiness markup requires a real business address. Keep this
  // disabled until a truthful address or public business profile is available.
  return null;
}
